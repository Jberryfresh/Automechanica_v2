{
  "name": "worker",
  "version": "0.1.0",
  "private": true,
  "main": "src/worker.ts",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/worker.ts",
    "start": "node dist/worker.js",
    "build": "tsc -p ."
  },
  "dependencies": {
    "amqplib": "^0.10.3",
    "dotenv": "^16.0.3",
    "openai": "^4.20.1"
  },
  "devDependencies": {
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.2.2",
    "@types/node": "^20.4.2"
  }
}
